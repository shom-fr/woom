# Custom configspec for the tiles example.
# Overrides the [[iters]] __many__ default (force_list) for the 'tile' key
# so that the gen_tiles validator extension is applied instead.

[ensemble]
    [[iters]]
    tile = gen_tiles(default=None)
